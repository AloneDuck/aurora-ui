# Contributing

Changes start with a failing contract or an explicit new scenario. Keep semantic tokens separate from primitives, preserve keyboard behavior, and include a Storybook state for visible UI changes.

Before opening a review, run `npm run lint`, `npm run typecheck`, `npm test`, `npm run build-storybook`, and `npm run test:e2e`.
