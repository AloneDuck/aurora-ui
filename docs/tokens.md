# Token model

Primitive tokens describe raw values. Semantic tokens describe intent such as canvas, surface, text, border, accent, and danger. Components consume semantic variables only, which allows themes to change without rewriting component selectors.

Token removals require a major release. Additions are minor; value corrections are patch releases when contrast and layout contracts remain compatible.
